// import { ITEMS_MAP } from '../../items/items';
import { FactionUpgrade, FactionUpgradeOptimization } from './upgrades.interface';

// const itemFactionUpgradeCost = (slug: string, quantity: number): FactionUpgradeCost => {
//   return { item: ITEMS_MAP.get(slug), quantity: quantity };
// };

export const TRAXUS_UPGRADES: FactionUpgrade[] = [];

export const TRAXUS_OPTIMIZATIONS: FactionUpgradeOptimization[] = [];
